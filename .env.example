# PostgreSQL connection string for the production signal database.
# Neon, Supabase, Railway Postgres, or another PostgreSQL provider can be used.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
