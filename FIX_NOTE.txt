LUMORA V14 MOBILE FIX

- Final responsive mobile layout.
- Removed desktop min-width / horizontal overflow on phones.
- Signal, stats, entry, chart, analysis and performance cards resize to the device width.
- Canvas chart scales correctly on mobile.
- Service worker cache bumped and HTML is network-first, so normal refresh gets new deployments.
- MT5/API logic unchanged.
- Trade history remains disabled.
